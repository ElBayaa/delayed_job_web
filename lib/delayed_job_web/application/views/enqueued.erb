<h1>Enqueued</h1>
<p class="sub">
  The list below contains all jobs currently in the delayed_job queue.
</p>
<p class="sub">
  <%= "Showing #{start+1} to #{start + per_page} of #{@all_jobs.count} enqueued jobs." %>
</p>
<%= partial :next_more, :start => start, :total_size => @all_jobs.count, :per_page => per_page %>
<ul class="job">
  <% @jobs.each do |job| %>
    <%= partial :job, {:job => job} %>
  <% end %>
</ul>
